pipeline {
    agent any

    environment {
        SCANNER_HOME = tool 'sonar-scanner'
        IMAGE_NAME = "nishantbadhiye07/shopverse-backend"
    }

    stages {

        stage('Cleaning Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout from Git') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/Nishantbadhiye/shopverse.git'
            }
        }

        stage('Sonarqube Analysis') {
            steps {
                dir('backend') {
                    withSonarQubeEnv('sonar-server') {
                        sh """
                        ${SCANNER_HOME}/bin/sonar-scanner \
                          -Dsonar.projectName=shopverse-backend \
                          -Dsonar.projectKey=shopverse-backend
                        """
                    }
                }
            }
        }

        stage('Trivy File Scan') {
            steps {
                dir('backend') {
                    sh '''
                    trivy fs --format table -o trivyfs.txt . | tee trivyfs.txt
                    '''
                }
            }
        }

        stage('Build and Push Image') {
            steps {
                script {

                    env.IMAGE_TAG = "build-${BUILD_NUMBER}"

                    dir('backend') {

                        withCredentials([usernamePassword(
                            credentialsId: 'docker-cred',
                            usernameVariable: 'DOCKER_USER',
                            passwordVariable: 'DOCKER_PASS'
                        )]) {

                            sh """
                            pwd

                            echo "===== Building Docker Image ====="
                            docker build -t ${IMAGE_NAME}:${IMAGE_TAG} .

                            docker tag ${IMAGE_NAME}:${IMAGE_TAG} ${IMAGE_NAME}:latest

                            echo "\$DOCKER_PASS" | docker login -u "\$DOCKER_USER" --password-stdin

                            docker push ${IMAGE_NAME}:${IMAGE_TAG}
                            docker push ${IMAGE_NAME}:latest

                            docker logout
                            """
                        }
                    }
                }
            }
        }

        stage('Trivy Image Scan') {
            steps {
                sh """
                trivy image --format table -o trivyimage.txt ${IMAGE_NAME}:${IMAGE_TAG}
                """
            }
        }

        stage('Update Deployment Manifest') {
            steps {
                dir('Kubernetes-Manifests-file/Backend') {

                    withCredentials([usernamePassword(
                        credentialsId: 'github-cred',
                        usernameVariable: 'GIT_USER',
                        passwordVariable: 'GIT_TOKEN'
                    )]) {

                        sh """
                        git config user.name "Nishantbadhiye"
                        git config user.email "nishantbadhiye09@gmail.com"

                        sed -i 's|image: .*|image: ${IMAGE_NAME}:${IMAGE_TAG}|g' deployment.yaml

                        git add deployment.yaml
                        git commit -m "Update backend image to ${IMAGE_TAG}" || true

                        git remote set-url origin https://${GIT_USER}:${GIT_TOKEN}@github.com/Nishantbadhiye/shopverse.git

                        git push origin main
                        """
                    }
                }
            }
        }
    }
post {
    always {
        mail(
            subject: "Pipeline Status: ${currentBuild.currentResult}",
            body: """
<html>
    <body>
        <h2>Jenkins Pipeline Notification</h2>

        <p><b>Build Status:</b> ${currentBuild.currentResult}</p>
        <p><b>Build Number:</b> ${env.BUILD_NUMBER}</p>
        <p><b>Job Name:</b> ${env.JOB_NAME}</p>

        <p>
            Check the
            <a href="${env.BUILD_URL}">Build Console Output</a>.
        </p>

        <br>

        <p>Regards,<br>Jenkins CI/CD</p>
    </body>
</html>
""",
            to: "nishantbadhiye07@gmail.com",
        
                
                mimeType: "text/html",
                attachmentsPattern: "backend/trivyfs.txt,trivyimage.txt"
            )
        }
    }
}
        
